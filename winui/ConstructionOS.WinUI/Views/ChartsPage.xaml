<Page
    x:Class="ConstructionOS.WinUI.Views.ChartsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI">
  <!-- U4 scaffold: wire LiveCharts to GET /api/kpi + /api/dashboard on Windows. -->
  <Grid RowDefinitions="Auto,*" Padding="8">
    <TextBlock Text="Charts (U4 scaffold)" Style="{StaticResource TitleTextBlockStyle}"/>
    <TextBlock x:Name="Status" Grid.Row="1" Margin="0,12,0,0"
               TextWrapping="Wrap"
               Text="Open on Windows against a running web_main.py backend. Bind LiveCharts series to KPI / cash / SPI-CPI series from the JSON API."/>
  </Grid>
</Page>
